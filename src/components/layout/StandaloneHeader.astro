---
// Header of the single-card pages: public verification and the company purchase.
import BrandLogo from './BrandLogo.astro';

interface Props {
  brandHref?: string;
}

const { brandHref } = Astro.props;
---

<header class="verify-header">
  <BrandLogo href={brandHref} />
  <slot />
</header>
